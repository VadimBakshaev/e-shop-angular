import { PasswordRepeat } from './password-repeat';

describe('PasswordRepeat', () => {
  it('should create an instance', () => {
    const directive = new PasswordRepeat();
    expect(directive).toBeTruthy();
  });
});
